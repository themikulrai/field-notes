build:
  docker:
    web: apps/api/Dockerfile
  config:
    DOCKER_BUILDKIT: "1"
run:
  web: uv run --package field-notes-api uvicorn field_notes_api.main:app --host 0.0.0.0 --port $PORT
release:
  command:
    - alembic -c apps/api/alembic.ini upgrade head
  image: web
